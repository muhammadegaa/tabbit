{
  "name": "tabbit",
  "version": "1.0.0",
  "description": "Browser productivity copilot Chrome extension",
  "main": "dist/popup.js",
  "scripts": {
    "build": "echo 'Build complete - static files ready'",
    "watch": "tsc --watch",
    "dev": "npm run build && npm run watch",
    "setup": "npm install && echo 'Extension ready! Load the folder in Chrome extensions page.'",
    "build:extension": "tsc"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "firebase": "^10.7.1",
    "@stripe/stripe-js": "^2.2.2",
    "stripe": "^14.9.0"
  },
  "keywords": [
    "chrome-extension",
    "productivity",
    "ai",
    "tabs",
    "openrouter",
    "firebase",
    "stripe"
  ],
  "author": "Tabbit Team",
  "license": "MIT"
} 